{
  "extends": "./tsconfig.json",
  "comment": "Build configuration for packaging and deployment; compile src only, excluding tests from dist/package output.",
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist", "test"]
}
